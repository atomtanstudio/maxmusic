node_modules/
.maxmusic-venv/
.maxmusic-tools/
__pycache__/
data/
.DS_Store
.env
shots/
shots/showcase/
render/out/
render/models/
render/data/job-*
render/data/model-sets-length.json
test-artifacts/
# Local source renders used to assemble public promo assets. These can exceed
# GitHub's per-file limit; only the selected compressed deliverables are kept.
docs/promo/**/source/
# Timestamped safety copies taken before a deploy or a bulk edit.
.maxmusic-backup-*/

# Internal working notes: they describe a private network and personal paths,
# and they are for building this rather than for installing it. Kept on disk.
RESUME.md
docs/BROKER.md
docs/CONTRACT.md
docs/SPEC.md
docs/backend-let-the-model-set-the-length.md
# The gauntlet monitor: a local QA dashboard with machine-specific paths.
gauntlet-status.mjs
docs/progress.html

# Personal permission settings, not part of the product.
.claude/settings.local.json
